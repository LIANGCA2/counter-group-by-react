export const increment = (index)=> ({type:'INCREMENT',index})
export const decrement = (index)=>({type:'DECREMENT',index})
export const mutiply = (mutipler,index)=>({type: 'MULTIPLIPLICATION',mutipler:mutipler,index})
export const deply = (index)=> ({type:'DEPLY',index})
